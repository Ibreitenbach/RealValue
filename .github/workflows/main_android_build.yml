name: Test Simple Workflow

on:
  push:
    branches:
      - main

jobs:
  simple_job:
    runs-on: ubuntu-latest
    steps:
      - name: Just Say Hello
        run: echo "Hello from GitHub Actions!"
