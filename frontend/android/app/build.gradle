plugins {
    id("com.android.application")
    id("org.jetbrains.kotlin.android")
}

android {
    namespace = "com.example.app" // Placeholder, may need adjustment
    compileSdk = rootProject.ext.compileSdkVersion

    defaultConfig {
        applicationId = "com.example.app" // Placeholder, may need adjustment
        minSdk = rootProject.ext.minSdkVersion
        targetSdk = rootProject.ext.targetSdkVersion
        versionCode = 1
        versionName = "1.0"
        vectorDrawables {
            useSupportLibrary = true
        }
    }

    buildTypes {
        release {
            minifyEnabled = false // Default for RN, can be true
            proguardFiles(getDefaultProguardFile("proguard-android-optimize.txt"), "proguard-rules.pro")
        }
    }

    compileOptions {
        sourceCompatibility = JavaVersion.VERSION_1_8
        targetCompatibility = JavaVersion.VERSION_1_8
    }

    kotlinOptions {
        jvmTarget = "1.8"
    }

    packagingOptions { // Common in React Native
        pickFirsts += [
            "lib/x86/libc++_shared.so",
            "lib/x86_64/libc++_shared.so",
            "lib/armeabi-v7a/libc++_shared.so",
            "lib/arm64-v8a/libc++_shared.so"
        ]
        jniLibs {
            useLegacyPackaging = true // May be needed for older RN versions or specific native modules
        }
    }
}

dependencies {
    // Kotlin
    implementation("org.jetbrains.kotlin:kotlin-stdlib:${rootProject.ext.kotlinVersion}")

    // AndroidX Core
    implementation("androidx.core:core-ktx:1.13.1") // Example, use appropriate version
    implementation("androidx.appcompat:appcompat:1.7.0") // Example

    // React Native
    implementation("com.facebook.react:react-android") // RN specific

    // Dependencies for React Native modules will be auto-linked or added here

    // Example: Hermes (if enabled)
    // if (isNewArchitectureEnabled()) {
    //     implementation("com.facebook.react:react-native-gradle-plugin")
    // } else {
    //     implementation("com.facebook.react:hermes-android")
    // }

    // Other dependencies
    // implementation("com.google.android.material:material:1.12.0") // Example
}

// Apply React Native specific Gradle configurations
if (project.hasProperty("react")) {
    apply(from = project.property("react") as String)
}
